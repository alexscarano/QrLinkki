# ==========================================
# Configuração do Ambiente (QrLinkki)
# ==========================================
# Instruções:
# 1. Copie este arquivo para ".env" na mesma pasta
# 2. Ajuste os valores conforme seu ambiente (Dev/Staging/Prod)
# 3. NUNCA commite o arquivo .env com senhas reais!

# ------------------------------------------
# 1. API Backend (OBRIGATÓRIO SEMPRE)
# ------------------------------------------
# Defina APENAS UMA das opções abaixo:

# A) Para Desenvolvimento (localhost da sua máquina):
# API_URL=http://192.168.1.10:5000

# B) Para Emulador Android (IP padrão do host):
# API_URL=http://10.0.2.2:5000

# C) Para Produção / Release Real:
API_URL=https://api.exemplo.com

# ------------------------------------------
# 2. Configuração de Build (OPCIONAL EM DEV)
# ------------------------------------------
# Estas variáveis SÓ são necessárias se você for rodar:
# - npm run build:android:release (Gera APK de produção)
# - npm run build:android:staging (Gera APK de teste)
#
# Se você só vai rodar "npm start" ou "npm run android", pode ignorar esta parte.

# Alias da chave no Keystore (padrão é 'qrlinkki_alias')
KEY_ALIAS=qrlinkki_alias

# Senhas do Keystore
# (Só preencha se for gerar APK de Release/Staging)
KEY_PASSWORD=senha_padrao_dev_123
KEYSTORE_PASSWORD=senha_padrao_dev_123

# Caminho opcional
# KEYSTORE_PATH=C:\Caminho\Seguro\minha-chave.jks

